services:
  - type: web
    name: cyberbully-chatbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn app:app --host 0.0.0.0 --port 8001
    envVars:
      - key: PORT
        value: 8001
